<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <title>Bookers</title>
  </head>
  <body>
    <% if flash[:notice] %>
      <p><%= flash[:notice] %></p>
    <% end %>

    <p><strong>Title:</strong>
      <%= @book.title %>
    </p>

    <p>
      <strong>Body:</strong>
      <%= @book.body %>
    </p>

    <%= link_to "Edit", edit_book_path(@book) %>
    <%= link_to "Back", "/books" %>
  </body>
</html>