<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <title>Bookers</title>
  </head>
  <body>
    <h1>Editing Book</h1>
    <% if @book.errors.any? %>
      <div id="error_explanation"><h3><%= @book.errors.count %> error prohibited this book from being saved:</h3>
      <% @book.errors.full_messages.each do |message| %>
        <ul><li><%= message %></li></ul>
      <% end %>
      </div>
    <% end %>


    <%= form_with model:@book, local: true do |f| %>

      <label for="book_title">Title</label>
      <%= f.text_field :title %>
      <label for="book_body">Body</label>
      <%= f.text_area :body %>
      <div></div>
      <%= f.submit 'Update Book' %>
    <% end %>


    <%= link_to "Show", booklist_path(@book.id) %> |
    <%= link_to "Back", "/books" %>
  </body>
</html>